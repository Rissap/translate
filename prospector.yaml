strictness: veryhigh

ignore-paths:
  - settings.py
  - manage.py

uses:
  - django

bandit:
  run: true

dodgy:
  run: true

mccabe:
  run: true

pycodestyle:
  run: true

pyflakes:
  run: true

pylint:
  run: true
  django-settings-module: server.settings
